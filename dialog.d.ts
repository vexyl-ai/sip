// Type shim for `@vexyl.ai/sip/dialog` — all types live in index.d.ts
import { dialog } from './index';

export { Dialog } from './index';
export type DialogOptions = dialog.DialogOptions;
export type AcceptOptions = dialog.AcceptOptions;
export type DialogStats = dialog.DialogStats;
